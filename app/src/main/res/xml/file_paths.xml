<?xml version="1.0" encoding="utf-8"?>
<paths>
    <!-- 下载安装包存放目录（app-specific external files dir/updates） -->
    <external-files-path
        name="updates"
        path="updates/" />
    <!-- 外部存储不可用时的内部降级目录（files dir/updates） -->
    <files-path
        name="updates_internal"
        path="updates/" />
</paths>
